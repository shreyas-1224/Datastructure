#include<stdio.h>

/*void keys(char * ,int );

void keys(char a[],int n){
	if(n<1){
		printf("reached");
		printf("%s\n",a);
		return ;
	}
	a[n-1]=0;
	keys(a,n-1);
	a[n-1]=1;
	keys(a,n-1);
	return;
}

int main(){
	int n;
	scanf("%d\n",&n);
	char s[n];
	keys(s,n);	
	return 1;
}
*/

//generate all keys of length n;
